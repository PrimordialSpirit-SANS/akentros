# 後端 /api 位址。apiFetch 會把請求送到 `${VITE_BEACON_API_BASE}${path}`,
# 例如後端 Worker 跑在本機 8787 埠時設為 http://127.0.0.1:8787/api。
# 留空時使用同源的 /api(可搭配部署平台的 reverse proxy 或 Pages functions)。
VITE_BEACON_API_BASE=/api

# 對外展示用的公開 API 根位址(顯示在「總覽」「快速開始」頁,
# 並作為 API Key 模式串流測試的端點)。留空時使用 window.location.origin。
# 例如:https://api.beacon.example
VITE_BEACON_PUBLIC_API_BASE=

# 設為 1 時啟用離線示範模式:攔截對 Beacon 後端的呼叫,
# 以記憶體內的假資料與模擬 SSE 串流回應(也可用網址參數 ?demo=1 臨時開啟)。
VITE_BEACON_DEMO=
